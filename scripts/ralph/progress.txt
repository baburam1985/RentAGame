## Codebase Patterns
- Components live in `web/src/components/` with co-located test files (`ComponentName.test.tsx`)
- Data types and static data in `web/src/data/games.ts`
- Pages in `web/src/app/` (Next.js App Router, "use client" for interactive pages)
- Test files use Vitest + Testing Library with `vi.fn()` for mocks and `userEvent` for interactions
- Components use controlled props pattern (value + onChange callback lifted to parent)
- Tailwind CSS only for styling; no hardcoded pixel values
- Filter components are stateless — parent (page.tsx) owns all filter state via useState
- Docker test image uses `node:20-bookworm-slim` (Debian), NOT Alpine — Alpine lacks `xvfb`/`udev` causing build failure
- E2E playwright.config.ts: skip `webServer` when `BASE_URL` env var is set (Docker CI uses http://app:3000)
- Playwright system Chromium: set `executablePath: process.env.CHROMIUM_PATH` when that env var is present
- Sort tests: pass a `games` prop with minimal mock data to GameGrid for deterministic ordering tests; use `getAllByRole("heading", { level: 3 })` to get card names in DOM order
- GameGrid accepts optional `games` override prop — lets tests inject controlled data without mocking modules
---

## 2026-04-13 - US-003 - Sort games by price or name
- What was implemented:
  - SortDropdown component: controlled `<select>` with aria-label="Sort", 4 options (featured/price-asc/price-desc/name-asc). Renders inline next to SearchBar in the filter row.
  - GameGrid updated: added optional `games` prop (defaults to imported games array) and `sortOrder` prop (defaults to "featured"). Sort applied after category+search filtering using a single `.sort()` with localeCompare for name-asc and arithmetic for price sort. Featured = preserve incoming order (return 0).
  - page.tsx wired: added `sortOrder` state (default "featured"), SortDropdown rendered alongside SearchBar, sortOrder passed to GameGrid.
- Files changed:
  - web/src/components/SortDropdown.tsx (new)
  - web/src/components/SortDropdown.test.tsx (new, 6 tests — RED committed first)
  - web/src/components/GameGrid.tsx (modified — added games+sortOrder props and sort logic)
  - web/src/components/GameGrid.test.tsx (modified — appended 6 sort tests to existing search tests)
  - web/src/app/page.tsx (modified — added SortDropdown import, sortOrder state, wiring)
- Learnings for future iterations:
  - To test sort order deterministically, pass a `games` prop with minimal mock data (3 games with distinct names and prices). Do NOT mock the module — use the optional prop override pattern.
  - Use `getAllByRole("heading", { level: 3 })` to get game card names in DOM order for sort assertions.
  - `[...array].sort(fn)` is important — never mutate the filtered array in-place as it could affect re-renders.
  - When appending tests to an existing test file, preserve the original describe block and add a new describe block. Never restructure existing passing tests.
---

## 2026-04-12 - US-001 - Search bar with live text filtering
- What was implemented:
  - SearchBar component: controlled text input with placeholder "Search games...", sr-only label for accessibility, conditional clear button (X icon) that resets query to empty
  - GameGrid updated: added optional `searchQuery` prop with case-insensitive filtering on game name and description; zero-results message changed to "No games found. Try a different search or category."
  - page.tsx wired: SearchBar rendered between CategoryFilter and GameGrid; searchQuery state lifted to Home component
- Files changed:
  - web/src/components/SearchBar.tsx (new)
  - web/src/components/SearchBar.test.tsx (new, 5 tests)
  - web/src/components/GameGrid.tsx (modified — added searchQuery prop and combined filter logic)
  - web/src/components/GameGrid.test.tsx (new, 6 tests)
  - web/src/app/page.tsx (modified — added SearchBar import, searchQuery state, wiring)
  - .gitignore (new — excludes node_modules, .next, tsbuildinfo, coverage, etc.)
- Learnings for future iterations:
  - GameGrid accepts searchQuery as optional string with default "" so existing usages don't break
  - Category and search filters are AND-combined in a single .filter() call
  - SearchBar uses aria-label "Clear search" for the clear button — tests rely on this exact accessible name
  - The "No games found" zero-state message must contain "no games found" (case-insensitive regex in tests)
  - Tests pass searchQuery directly to GameGrid rather than simulating typing — they test the filtering logic, not the input wiring

## 2026-04-12 - US-001 - qa-failed fix (Docker CI environment)
- Root cause of CI failures (Unit Tests + E2E Tests both failed, Docker Build passed):
  - Dockerfile.test used `node:20-alpine` which does NOT have `xvfb` or `udev` Alpine packages.
    The `apk add xvfb udev` step failed with "package not found", causing the Docker image build
    to error out. Since both unit-tests and e2e-tests use Dockerfile.test, both CI jobs failed.
  - playwright.config.ts had an unconditional `webServer` that tried to start `npm run dev` on
    localhost:3000, even in Docker CI where the app runs at http://app:3000 (separate service).
- What was fixed:
  - Dockerfile.test: switched base image to `node:20-bookworm-slim` (Debian). Replaced `apk add`
    with `apt-get install chromium fonts-freefont-ttf ca-certificates`. Debian has official
    Playwright support. Corrected CHROMIUM_PATH to `/usr/bin/chromium` (Debian binary location).
  - playwright.config.ts: added `useExternalServer` flag based on `BASE_URL` env var. When
    `BASE_URL` is set, `webServer` block is omitted so Playwright doesn't try to start a local
    dev server. Also wired `CHROMIUM_PATH` env var to `executablePath` in the chromium project.
- Files changed:
  - web/Dockerfile.test
  - web/playwright.config.ts
- Gotcha: Alpine Linux (`node:*-alpine`) does NOT have `xvfb` — use `xorg-server-xvfb` or
  switch to Debian. Playwright officially doesn't support Alpine for E2E. Always use
  `node:20-bookworm-slim` or `node:20` for test images that need a browser.
- Gotcha: `CHROMIUM_PATH` is not a Playwright built-in env var — must be read and set as
  `executablePath` in the playwright.config.ts project definition.
---

## [2026-04-12] - US-002 - Price range slider filter
- What was implemented:
  - PriceRangeSlider component: dual-handle slider using two overlaid native `<input type="range">` elements (no external library). Includes onKeyDown handlers for ArrowRight/ArrowLeft/ArrowUp/ArrowDown to ensure Testing Library userEvent compatibility. Price range label rendered as a single template-literal text node (`$25 – $50 / day`) — do NOT use nested `<span>` elements or JSX text interpolation that splits the string, as Testing Library's getByText regex matcher requires the full string in one text node.
  - GameGrid updated: added optional `minPrice` and `maxPrice` props; all three filters (category, search, price) are AND-combined in a single `.filter()` pass.
  - page.tsx updated: added PriceRangeSlider import, PRICE_MIN/PRICE_MAX constants derived from games array, minPrice/maxPrice state initialized to full range, all passed down to GameGrid. SearchBar logic also moved inline into page.tsx (no separate SearchBar component import needed).
- Files changed:
  - web/src/components/PriceRangeSlider.tsx (new)
  - web/src/components/PriceRangeSlider.test.tsx (new, 7 tests — RED committed first)
  - web/src/components/GameGrid.tsx (modified — added minPrice/maxPrice props)
  - web/src/components/GameGrid.test.tsx (new, 13 tests including price filter tests — RED committed first)
  - web/src/app/page.tsx (modified — wired PriceRangeSlider + search state)
- Learnings/patterns discovered:
  - Testing Library `getByText` with a regex requires the matched text to live in a SINGLE DOM text node. If the display string is split across elements (e.g., `$30 <span>/ day</span>`), the regex will not match — use a template literal in a single `<span>` instead.
  - `userEvent.type(input, "{arrowright}")` fires `keydown`/`keypress`/`keyup` events but does NOT fire the native `change` event on a range input in jsdom. Must add an `onKeyDown` handler that manually calls the change callback.
  - Range input `onChange` does fire correctly when using `fireEvent.change(input, {target: {value: '30'}})`, but arrow keys require the explicit keyboard handler approach.
  - Price filter state (minPrice/maxPrice) defaults to the actual min/max of the games catalog — derive with `Math.min/max(...games.map(g => g.pricePerDay))` at module scope to avoid recalculation on every render.
---

## 2026-04-12 - US-001 - qa-failed fix (true root cause: .dockerignore excludes test files)
- Root cause of persistent Docker CI failures (Unit Tests failing after EVERY Dockerfile fix):
  - web/.dockerignore contains: `*.test.ts`, `*.test.tsx`, `vitest.config.ts`, `playwright.config.ts`
  - These are excluded from the production Docker image (correct). But Docker uses the SAME
    .dockerignore for ALL Dockerfiles built with the same context directory.
  - Result: `Dockerfile.unit-tests` built with context `./web` → test files and vitest.config.ts
    are NOT copied into the container → vitest finds 0 test files → exits with error code 1.
  - Previous "root cause" about musl/glibc Rolldown binary was a misdiagnosis. Tests actually
    pass fine with npm ci and both gnu+musl binaries present (Rolldown picks the right one at runtime).
- What was fixed:
  - Added `web/Dockerfile.unit-tests.dockerignore` — same as .dockerignore but WITHOUT test
    file and config exclusions. Docker BuildKit's per-Dockerfile dockerignore feature uses
    `<dockerfile>.dockerignore` when it exists, overriding the global `.dockerignore`.
  - Added `web/Dockerfile.e2e-tests.dockerignore` — same approach for e2e tests.
  - Reverted both test Dockerfiles back to `npm ci` (lock file install) — deterministic and
    no registry calls needed. The `npm install` without lock file approach risked resolving
    different package versions or failing if fictional package versions aren't on the registry.
- Files changed:
  - web/Dockerfile.unit-tests (reverted to npm ci)
  - web/Dockerfile.e2e-tests (reverted to npm ci)
  - web/Dockerfile.unit-tests.dockerignore (new)
  - web/Dockerfile.e2e-tests.dockerignore (new)
- CRITICAL PATTERN: Docker per-Dockerfile .dockerignore:
  `Dockerfile.foo` → Docker BuildKit looks for `Dockerfile.foo.dockerignore` first.
  If found, it REPLACES (not merges with) the global `.dockerignore` for that build.
  Always create per-Dockerfile .dockerignore files for test Dockerfiles when the project
  has a .dockerignore that excludes test files (which is correct for production images).
---
